<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="pragma" content="no-cache">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página Inicial</title>
</head>
<body>
    <h1><%= titulo %></h1>
    <h3>Online no IP:<%= info.host_ip %></h3>

    <p><%= conteudo %></p>

	<p>Número Aleatório identificador deste POD:<%= info.numero %></p>

    <div style="background-color: azure;padding: 20px;">
        <p><bold>IP da Requisição:</bold><%= info.ip %></p>
        <p><bold>Caminho:</bold><%= info.path %></p>
        <p><bold>User Agent:</bold><%= info.user_agent %></p>
    </div>

    <form action="<%=info.publicpath%>terminal" method="POST">
        <div>
          <label for="cmd">></label>
          <textarea id="cmd" name="cmd" rows="4" cols="120"></textarea>
        </div>
        <div>
          <button type="submit">Executar</button>
        </div>
      </form>
      <p>Saída:</p>
      <textarea rows="40" cols="120">
        <% if(locals.cmd) { %>
            <%= locals.cmd %>
        <% } %>
      </textarea>
      
</body>
</html>