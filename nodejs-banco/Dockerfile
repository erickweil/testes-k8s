FROM node:18
WORKDIR /app-node
ARG PORT_BUILD=3000
ENV PORT=$PORT_BUILD
ENV MONGODB_HOST="mongodb"
ENV TITULO="CRIADO PELO DOCKER"
EXPOSE $PORT_BUILD
COPY . .
RUN npm install
ENTRYPOINT npm run devStart